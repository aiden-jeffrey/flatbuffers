# GdScript tests

For now there's a "test" runner shell script (gd_tests.sh) that generates code, generates a
reference binary using the ts bindings (for the simple schema), and then just prints a load
of things to stdout.

## TODO:

- write proper tests
